import React from "react";
import { Container, Typography } from "@mui/material";

const Home = () => {
  return (
    <Container>
      <Typography variant="h3">Welcome to Smart Classroom</Typography>
    </Container>
  );
};

export default Home;
